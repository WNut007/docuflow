@{
    ViewData["Title"] = "Upload";
}
<div class="row justify-content-center">
    <div class="col-md-7">
        <h1 class="h3 mb-3">Upload document</h1>
        <div asp-validation-summary="All" class="text-danger small mb-2"></div>

        <div class="card">
            <div class="card-body">
                <form method="post" asp-action="Upload" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()
                    <div class="mb-3">
                        <label class="form-label">Document file (PDF / image)</label>
                        <input type="file" name="file" class="form-control" accept=".pdf,.png,.jpg,.jpeg,.tiff" required />
                        <div class="form-text">
                            On upload the pipeline runs: classify → OCR (text + tables) → map to model.
                        </div>
                    </div>
                    <button class="btn btn-primary" type="submit">
                        <i class="bi bi-play-fill"></i> Upload & process
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
