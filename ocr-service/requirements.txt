# Runtime deps LAYERED ON the official `paddlepaddle/paddle:3.0.0` base (see Dockerfile). That base ships
# a WORKING paddle 3.0.0 with a matched glibc/MKL/protobuf toolchain — the plain pip `paddlepaddle` wheel
# on a slim base segfaults the moment paddle runs an op, so we do NOT pip-install paddlepaddle here.
#
# UPGRADED 2.7.3 -> 3.x for PP-OCRv5 (real Thai recognition, proven). OCR-ONLY branch: we serve /ocr via
# PaddleOCR.predict only; the PP-Structure table path is NOT shipped here (it stays parked).
#   paddleocr 3.x        PP-OCRv5 engine; .predict() API
#   paddlex[ocr]         kept from the PROVEN pin (lowest build risk). paddleocr 3.x depends on paddlex
#                        regardless; the [ocr] extra installs CODE, not models, so with no PPStructureV3
#                        warm-up nothing extra is downloaded and the image stays lean.
# The old 2.6.2-era pins (numpy<2, single opencv-contrib) are GONE: paddle 3.x is numpy-2 ready and the
# 3.x install resolves opencv cleanly (verified — PaddleOCR runs without the normalize step).
paddleocr>=3.0,<4
paddlex[ocr]==3.7.2
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pillow==10.3.0
